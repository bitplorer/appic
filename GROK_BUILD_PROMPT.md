# GROK BUILD MASTER — APPIC on ux-compose (locked)

Identity: Grok Build operating system for APPIC.
Audience: Grok Build only. Copy everything below the first horizontal rule. Execute. Do not ask the user to run local commands.

NORTH_STAR: Ship a running nocturnal foundry OS on GitHub bitplorer/appic whose every visible room is authored only through ux_compose public names, every kit stem is owned and live, every Clock A payload door works, Caps fail closed, motion is Morph-then-Play, and no React/JSX/TSX/Vite/Nitro product path exists.

Residual risk: the Grok sandbox defaults to TanStack Start / Vite. That path already failed this session (CssSyntaxError: Unknown word export on assets/css/*.css during nitro/vercel build). If the agent just builds the sandbox, APPIC dies.

NORTH_STAR Kill Trigger: abort SHIP if any product UI is React/Vue/Svelte/Next/TanStack/JSX/TSX, if any CSS file begins with or contains a JS export statement, if uxcompose serve is replaced by vite build, or if a page unit grows HTTP verbs (get/post).

Antifragility Pair:
1. When the sandbox injects src/router.tsx, leave it unused and keep startup.sh bound to uvicorn app:asgi or appic.server:app on 0.0.0.0:8080.
2. When Tailwind CLI is missing, hand-author complete assets/static/file/css/output.css and still call WebAssets.mount_css. Do not invent a Vite CSS pipeline.

Library wins. Product HEAD to consume: bitplorer/appic@f4aeab2ef26c189e52d07186ebae51ec633d5941. Library pin: bitplorer/ux-compose@6b84972bf9a6f9508321ef929bff6f18307e7632 (0.1.0). If code and this prompt disagree, code wins. Do not invent APIs.

---

## §0 MISSION

Build and publish APPIC as a complete product, not a widget zoo. The document is the composition root made visible. Caps are seals. Intent is a nucleus. Ops are traces. Routes are a constellation. Skin is WebAssets. GET is Clock A. Action is Clock B. Payload type picks media type. Kit cards are rooms you own. Signal is a grammar you can feel. Relay is how serve actually works. Every name in ux_compose.__all__ appears in product source. Push the running tree to bitplorer/appic on main. Leave uvicorn listening on 0.0.0.0:8080.

Success is NOT: a TanStack preview that typechecks; a CSS file that Vite can parse; a gallery of unowned from ux_compose.kit import cards; a README that lists features the routes do not walk.

## §1 A-TIER

- Every public ux_compose name used in a live room.
- 23 kit stems copied under components/, restyled, host seams overridden, each visible in Door/Desk/House/Visit/Signal.
- Clock A proven: /health dict JSON, /pulse generator stream, /atelier/{sku} HTML with no get().
- Caps fail closed without mint and succeed after host mint.
- Isolation doctor green on appic/ + components/.
- No product .tsx/.jsx. No CSS export. No host="batteries".
- Desktop + 390px render with clean console.
- FEATURE_INVENTORY.md stays truthful against the vendored SHA.

## §2 GAP MAP

Cartography proof (tree wins):
- Repo bitplorer/appic EXISTS. Do not create. Default branch main. HEAD f4aeab2.
- Already present: GROK_BUILD_PROMPT.md, FEATURE_INVENTORY.md, app.py, settings.py, document.py, components/ (23 stems), appic/routes/* including health pulse clocks relay signal enter desk house visit, vendor trees, assets/css/input.css, startup.sh.
- Library bitplorer/ux-compose EXISTS. Public surface listed in FEATURE_INVENTORY.md.
- CLI: uxcompose create-app | serve dev|prod|restart-channel | build | deploy | doctor | add.
- Observed failure: Vite/Nitro treated CSS as JS (Unknown word export). Forbidden to re-enter.
- Stale docs are gaps. Re-read ux_compose/__init__.py __all__ and kit/catalog.py before writing rooms.

## §3 LAYER LAW

1. Product UI = Python + ux-compose only. Never import ux_channel from product modules.
2. Page units have no HTTP verbs. Extra APIs live on the FastAPI process build() returned.
3. CSS files are CSS. First token is a CSS token or at-rule. Never JS export.
4. HMR/tunnel are uxcompose serve dev delivery, not Document APIs.
5. Kit: copy then own. Do not ship library kit imports as the running card.
6. Motion: plans carry no html=. Morph first, then transition.play.
7. Encoding: qualitative MorphState; magnitudes on RefState + stamp.
8. Sandbox platform files may exist unused. They are not the product. Do not run vite build / nitro / tsc as the ship gate.

## §4 CONSUME / ADAPT / KEEP / REPLACE

CONSUME existing APPIC rooms, owned kit copies, vendor trees, inventory, startup.sh.
ADAPT rooms that still import ux_compose.kit as the live card; CSS polluted with export; leftover TanStack routes.
KEEP nocturnal palette, type stack, 23 stems, Clock A doors, Relay, Signal grammar.
REPLACE any React scaffold that became the preview; host=batteries teaching; invented sixth products.

## §5 INVARIANTS

NORTH_STAR as locked. use_htmx=False. Auth OFF for Grok accounts. Kit Login/Otp are foundry Caps against Host store. Database OFF. Serve 0.0.0.0:8080. Keep Created-with-Grok pill.
Caps: orders.place orders.coupon identity.verify calendar.book settings.wipe comments.moderate admin.reset lattice.mint auth.login auth.signup auth.otp flow.finish rows.archive.
Coupons HOUSE/FLAX/TABLE. OTP accept 2048, refuse 000000. Login refuse @blocked.test.
raw() once for a safe SVG mark, not CSS. morph_play once (Lab). update_with everywhere else. bind() on Pulse and Lattice mint.

## §6 WORKSTREAMS

Concurrency: parallel research, sequential ship, Critic last.
W0 Cartographer (sequential first): re-read library + appic tree. Paste proof. Kill if names invented.
W1 Product Author (parallel with W2 after W0): repair rooms so coverage matrix is true.
W2 Skin + Delivery (parallel with W1): tokens, WebAssets, startup.sh, no Vite. Prove CSS parses as CSS.
W3 Publisher (sequential): push to bitplorer/appic main. Do not create a second repo.
W4 Critic (independent): score every plane. SHIP only if all >= 4.

## §7 T1 BAR

uvicorn binds 8080 and renders /. GET /health is JSON. GET /pulse streams. GET /atelier/tee is HTML. Isolation scan clean. 23 stems on Trace. Signal synthesizers live. 390px: no overflow, 44px targets.

## §8 CRITIC

Independent plane. Scores W0-W3 on truth-to-library, isolation, Clock A, kit ownership, no-React, CSS purity 0-5. Residual disagreement map required. SHIP iff every plane >= 4. Kill on invented names or CSS export.

## §9 /LOOP

Max 8 loops. Phase 2 (tunnel/deploy providers) is not required for Phase 1 SHIP.

## §10 PROOFS

Paste: __all__ names used; kit stems vs components/; isolation scan; /health body; first bytes of input.css/output.css (must not be export); git SHA pushed.

## §11 DONE WHEN

Coverage matrix checked. https://github.com/bitplorer/appic updated. Uvicorn up. Residuals stated. Self-Inversion Hook run.
Success is NOT soak tests, not tsc --noEmit, not Vercel nitro output.
Forbidden to reintroduce: React Vue Svelte Next TanStack Start as product, HTMX architecture, host=batteries, quantity MorphState, kit library imports as shipped cards, root-level swipe, CSS-in-Python-strings, JS export in CSS, page-unit HTTP verbs.
Never ask the user to run these.

## §12 STYLE

Staff+ voice. Paths over adjectives. No consider / feel free. Editorial nocturnal foundry. No purple gold neon emoji chrome.

Self-Inversion Hook: invert this NORTH_STAR. If stunning app can be satisfied by a Vite skin over unused Python, the master failed. If Grok Build ships the foundry but the sandbox preview still runs TanStack, name that residual and keep startup.sh as the real process.

---

## PUBLIC SURFACE (do not invent)

App, build, WebAssets, DirectoryRoutes, DirectoryASGI, RouterHooks, Surface, SurfaceBundle, SurfaceError, mount_surfaces, scan_surfaces, validate_surfaces, Component, MorphState, RefState, action, bind, control, notify, update_with, morph_play, Level, doctor, DoctorResult, scene, fade, rise, slide, HAS_DOM, raw, __version__, html, head, body, title, style, meta, link, script, div, span, h1, h2, h3, p, a, button, form, input_, ul, li, header, footer, aside, section, article, nav, main, label, svg, path, rect, circle.

Also from compose, never ux_channel: ActionInfo, BuildResult, RouteRecord, DirectoryRoutesError, HMR_PATH, attach_hmr, client_script_tag, IsolationViolation, CSS_URL_PREFIX, OUTPUT_CSS_NAME, http_path, is_json_payload, is_stream_payload, apply_html_document.

Kit catalog for add + Trace, not shipped import: KitEntry, CATALOG, list_components, resolve, copy_component, find_app_root, KitCopyError.

23 stems: login tabs accordion dropdown dialog sheet toast command table pagination combobox sidebar breadcrumb stepper carousel calendar select otp plans actionsheet contextmenu typeahead pullrefresh.

## BUILD DOOR

from pathlib import Path
from ux_compose import build
from document import document
from settings import webassets
PACKAGE = Path(__file__).resolve().parent
app, asgi, bundle = build(PACKAGE, name="APPIC", host="auto", live="auto", level="auto", base="routes", fail_closed=False, use_htmx=False, document=document)
asgi = webassets.mount_css(asgi)

Install specialists from bitplorer git remotes. Vendor under vendor/ux-compose-src and vendor/specialists-src. PYTHONPATH=.pydeps:vendor/ux-compose-src:vendor/specialists-src:.
uxcompose add --force every stem listed above.

## CLOCK A

render() tag/str/bytes -> HTMLResponse wrapped. dict -> JSON unwrapped. generator -> StreamingResponse unwrapped. Response -> as-is. str is not a stream. routes/atelier/[sku].py class Sku def render(self, sku: str = "").

## SIGNAL

swipe.vertical swipe.horizontal click swipe.down click swipe.right longpress input delay:
Swipe on handle / Keep it / Close, never root. Typeahead morphs #{id}-hits only.

## ROOMS

/ Table, /enter Door, /desk Desk, /house House, /visit Visit, /signal Signal, /atelier + /atelier/{sku}, /commission, /bag, /board, /studio, /ledger, /lab, /lattice, /trace, /clocks, /relay, /health, /pulse.
Chrome: Toasts Palette Banner Ribbon. Cmd-K posts POST /action/{name}.
Extra process routes only: POST /action/{name}, GET /api/surfaces, GET /api/kit, static /css, /public.

## COVERAGE MATRIX

Every __all__ name used. 23 owned kit files live. Clock A three doors. Relay names serve-dev / serve-prod / restart-channel. Caps fail closed then succeed. scene.share atelier to bag. RedirectResponse after checkout. Isolation clean. CSS contains no export. No product tsx. startup.sh 0.0.0.0:8080. FEATURE_INVENTORY.md + this prompt in repo. Custom OG. 390px no overflow.

## PALETTE

Ink #0c0d0b elevated #141512 surface #1a1b18 bone #ebe6d8 muted #9a9488 cool #c8ccd4 danger #c17a6e ok #8fa394.

Phase 1 SHIP = running foundry on GitHub + uvicorn. Phase 2 = tunnel/deploy. Phase 2 is not a dependency of Phase 1.
